<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#5B00BCD4"
    tools:context=".OpenCloseActivity">

    <EditText
        android:id="@+id/carModelEditText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="42dp"
        android:ems="10"
        android:hint="Car Model"
        android:inputType="textPersonName"
        android:minHeight="48dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/button4"
        android:layout_width="91dp"
        android:layout_height="60dp"
        android:layout_marginTop="24dp"
        android:backgroundTint="#4CAF50"
        android:onClick="openCar"
        android:text="Open"
        app:layout_constraintStart_toStartOf="@+id/carModelEditText"
        app:layout_constraintTop_toBottomOf="@+id/carModelEditText" />

    <Button
        android:id="@+id/closeButton"
        android:layout_width="95dp"
        android:layout_height="61dp"
        android:layout_marginTop="24dp"
        android:backgroundTint="#E91E63"
        android:onClick="closeCar"
        android:text="Close"
        app:layout_constraintEnd_toEndOf="@+id/carModelEditText"
        app:layout_constraintTop_toBottomOf="@+id/carModelEditText" />
</androidx.constraintlayout.widget.ConstraintLayout>